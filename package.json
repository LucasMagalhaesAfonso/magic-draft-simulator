{
  "dependencies": {
    "@anthropic-ai/sdk": "^0.74.0",
    "dotenv": "^17.3.1",
    "playwright": "^1.58.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "start": "npx http-server . -p 8080 -c-1",
    "test": "echo '❌ Tests disabled. See CLAUDE.md for manual testing'",
    "test:manual": "npx playwright test",
    "test:report": "npx playwright show-report",
    "test:layer": "npx playwright test --project"
  }
}
