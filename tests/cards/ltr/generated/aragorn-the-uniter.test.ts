import { describe, it, expect } from 'vitest';
import { TestGame, CardUtils } from '../../../helpers/game-helper';
import { CardEffectsDB } from '../../../../src/engine/card-effects';

// AUTO-GENERATED by tools/generate-runtime-tests.ts
// Tests runtime behavior via real engine calls (not just DB structure checks)

describe("Aragorn, The Uniter", () => {

  it('exists in CardEffectsDB', () => {
    expect(CardEffectsDB["aragorn, the uniter"]).toBeDefined();
  });

  it('has "cast_spell" trigger in DB', () => {
    const db = CardEffectsDB['aragorn, the uniter'];
    const trigger = db.triggered?.find((t: any) => t.event === "cast_spell");
    expect(trigger).toBeDefined();
  });

  it('[trigger:cast_spell] creates 1 Human Soldier token(s)', () => {
    const game = new TestGame();
    game.resolveEffect(0, {"type":"create_token","power":1,"toughness":1,"name":"Human Soldier","type_line":"Creature — Human Soldier","colors":["W"]});
    const tokens = game.battlefield(0).filter((c: any) => c.name === "Human Soldier");
    expect(tokens.length).toBe(1);
  });

  it('has "cast_spell" trigger in DB', () => {
    const db = CardEffectsDB['aragorn, the uniter'];
    const trigger = db.triggered?.find((t: any) => t.event === "cast_spell");
    expect(trigger).toBeDefined();
  });

  it('[trigger:cast_spell] scry 2 — sets _pendingScry for human', () => {
    const game = new TestGame();
    game.setHuman(0, true);
    for (let i = 0; i < 3; i++) game.addToLibraryTop(0, { name: 'Top', type_line: 'Creature' });
    game.resolveEffect(0, {"type":"scry","amount":2});
    // Human gets a scry overlay
    const wfi = game.state.waitingForInput;
    expect(wfi?.type === 'scry' || game.state._pendingScry != null).toBe(true);
  });

  it('has "cast_spell" trigger in DB', () => {
    const db = CardEffectsDB['aragorn, the uniter'];
    const trigger = db.triggered?.find((t: any) => t.event === "cast_spell");
    expect(trigger).toBeDefined();
  });

  it('[trigger:cast_spell] deals 3 damage to player', () => {
    const game = new TestGame();
    game.setLife(1, 20);
    game.resolveEffect(0, {"type":"damage","amount":3,"target":"opponent"});
    expect(game.life(1)).toBe(20 - 3);
  });

  it('has "cast_spell" trigger in DB', () => {
    const db = CardEffectsDB['aragorn, the uniter'];
    const trigger = db.triggered?.find((t: any) => t.event === "cast_spell");
    expect(trigger).toBeDefined();
  });

  it('[trigger:cast_spell] buffs creature +4/+4 (end_of_turn)', () => {
    const game = new TestGame();
    const creature = game.addToBattlefield(0, { name: 'Target', type_line: 'Creature', power: '1', toughness: '1' });
    game.resolveEffect(0, {"type":"buff","power":4,"toughness":4,"target":"creature","duration":"end_of_turn"});
    expect(CardUtils.getPower(creature)).toBeGreaterThanOrEqual(1 + 4);
    expect(CardUtils.getToughness(creature)).toBeGreaterThanOrEqual(1 + 4);
  });

  describe('Cast flow', () => {
    it('AI can cast without crash', () => {
      const game = new TestGame();
      // Give AI plenty of mana
      game.setMana(0, { W: 5, U: 5, B: 5, R: 5, G: 5, C: 10 });
      // Add cards to hand if needed for costs
      for (let i = 0; i < 3; i++) game.addToHand(0, { name: 'Filler', type_line: 'Creature', cmc: 0 });
      for (let i = 0; i < 5; i++) game.addToLibraryTop(0, { name: 'Lib', type_line: 'Creature' });
      for (let i = 0; i < 3; i++) game.addToLibraryTop(1, { name: 'Lib', type_line: 'Creature' });
      // Add opponent creature (target for damage/destroy effects)
      game.addToBattlefield(1, { name: 'Opponent Creature', type_line: 'Creature — Beast', power: '2', toughness: '4' });
      const card = game.addToHand(0, { name: "aragorn, the uniter", type_line: 'Instant', mana_cost: '{1}', cmc: 1 });
      expect(() => game.cast(0, card._uid)).not.toThrow();
    });
  });
});
