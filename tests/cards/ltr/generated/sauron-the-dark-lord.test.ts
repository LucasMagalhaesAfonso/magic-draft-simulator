import { describe, it, expect } from 'vitest';
import { TestGame, CardUtils } from '../../../helpers/game-helper';
import { CardEffectsDB } from '../../../../src/engine/card-effects';

// AUTO-GENERATED by tools/generate-runtime-tests.ts
// Tests runtime behavior via real engine calls (not just DB structure checks)

describe("Sauron, The Dark Lord", () => {

  it('exists in CardEffectsDB', () => {
    expect(CardEffectsDB["sauron, the dark lord"]).toBeDefined();
  });

  it('has "opponent_casts_spell" trigger in DB', () => {
    const db = CardEffectsDB['sauron, the dark lord'];
    const trigger = db.triggered?.find((t: any) => t.event === "opponent_casts_spell");
    expect(trigger).toBeDefined();
  });

  it('[trigger:opponent_casts_spell] amasses 1 (creates/grows Orc Army)', () => {
    const game = new TestGame();
    game.resolveEffect(0, {"type":"amass","amount":1});
    const armies = game.battlefield(0).filter((c: any) => (c.type_line||'').toLowerCase().includes('army'));
    expect(armies.length).toBe(1);
    expect(armies[0]._counters?.['+1/+1'] || 0).toBe(1);
  });

  it('has "combat_damage_player" trigger in DB', () => {
    const db = CardEffectsDB['sauron, the dark lord'];
    const trigger = db.triggered?.find((t: any) => t.event === "combat_damage_player");
    expect(trigger).toBeDefined();
  });

  it('[trigger:combat_damage_player] has ring_tempts effect in DB', () => {
    const db = CardEffectsDB['sauron, the dark lord'];
    const json = JSON.stringify(db);
    expect(json).toContain("ring_tempts");
  });

  it('has "ring_tempts" trigger in DB', () => {
    const db = CardEffectsDB['sauron, the dark lord'];
    const trigger = db.triggered?.find((t: any) => t.event === "ring_tempts");
    expect(trigger).toBeDefined();
  });

  it('[trigger:ring_tempts] has loot effect in DB', () => {
    const db = CardEffectsDB['sauron, the dark lord'];
    const json = JSON.stringify(db);
    expect(json).toContain("loot");
  });

  describe('Cast flow', () => {
    it('AI can cast without crash', () => {
      const game = new TestGame();
      // Give AI plenty of mana
      game.setMana(0, { W: 5, U: 5, B: 5, R: 5, G: 5, C: 10 });
      // Add cards to hand if needed for costs
      for (let i = 0; i < 3; i++) game.addToHand(0, { name: 'Filler', type_line: 'Creature', cmc: 0 });
      for (let i = 0; i < 5; i++) game.addToLibraryTop(0, { name: 'Lib', type_line: 'Creature' });
      for (let i = 0; i < 3; i++) game.addToLibraryTop(1, { name: 'Lib', type_line: 'Creature' });
      // Add opponent creature (target for damage/destroy effects)
      game.addToBattlefield(1, { name: 'Opponent Creature', type_line: 'Creature — Beast', power: '2', toughness: '4' });
      const card = game.addToHand(0, { name: "sauron, the dark lord", type_line: 'Instant', mana_cost: '{1}', cmc: 1 });
      expect(() => game.cast(0, card._uid)).not.toThrow();
    });
  });
});
